import { expect, test } from "@jest/globals";
// import { getTimerDisplayVal } from "./app";

// test("returns equivalent string if number already has two digits", () => {
//   const res = getTimerDisplayVal(10);
//   expect(res).toBe("10");
// });

test("adds 1 + 2 to equal 3", () => {
  expect(3).toBe(3);
});
